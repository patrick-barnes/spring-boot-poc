# spring-boot-poc

Minimal Spring Boot "Hello World" app.

How to build and run (Windows PowerShell):

```powershell
cd c:\git\spring-boot-poc
mvn package
mvn spring-boot:run
```

Then open http://localhost:8080/ to see "Hello, World!".
